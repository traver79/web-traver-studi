---
import {withBase} from '../utils/i18n';
---
<!-- Compact viewport of the supplied vectors. Original paths, relative positions,
     lettering and proportions are untouched; only the outer seal is omitted here. -->
<svg viewBox="210 820 1620 555" width="1620" height="555" fill="currentColor" aria-hidden="true" focusable="false">
  <use href={`${withBase('brand/logo.svg')}#traver-studi`}/>
  <use href={`${withBase('brand/logo.svg')}#interiorisme`}/>
</svg>
